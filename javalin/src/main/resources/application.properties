# Override any value with env vars like ORDER_SERVER_PORT
# Server
order.server.port=8080

# Database
order.db.url=jdbc:postgresql://localhost:5432/order
order.db.user=order
order.db.password=order
order.db.max.pool.size=10
order.db.pool.name=linjava-pool

# Migrations
order. javalin/db/migration
